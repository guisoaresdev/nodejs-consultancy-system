/**
 * Classe que define os parâmetros de acesso ao SGBD
 */
const dbConfig = {
  database: "consultorio",
  username: "postgres",
  password: "12345",
  host: "localhost",
  dialect: "postgres",
};

export default dbConfig;
